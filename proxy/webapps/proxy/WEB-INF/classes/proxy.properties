# HTTP反向代理配置文件

# 目标服务器URL（必须配置）
proxy.target.url=http://www.baidu.com

# 连接超时时间（毫秒）
proxy.connect.timeout=30000

# 读取超时时间（毫秒）
proxy.read.timeout=30000

# 缓冲区大小（字节）
proxy.buffer.size=8192

# 是否启用调试日志
proxy.debug.enabled=false